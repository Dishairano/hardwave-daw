[package]
name = "hardwave-native-plugins"
version.workspace = true
edition.workspace = true

[dependencies]
hardwave-engine = { path = "../hardwave-engine" }
hardwave-dsp = { path = "../hardwave-dsp" }
hardwave-plugin-host = { path = "../hardwave-plugin-host" }
hardwave-midi = { path = "../hardwave-midi" }
raw-window-handle.workspace = true
serde.workspace = true
log.workspace = true

[dev-dependencies]
# Render-to-WAV proof test needs project types (PluginSlot, clips) to build a
# project with an insert, and hound to write the rendered master to disk.
hardwave-project = { path = "../hardwave-project" }
hound.workspace = true
